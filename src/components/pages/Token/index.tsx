import { ReactChild, ReactFragment, ReactPortal } from "react";
import * as Layouts from "../../Layouts";
const TokenPage = () => {
  return <>Token page</>;
};
TokenPage.layout = Layouts.SecondLayout;

export default TokenPage;
